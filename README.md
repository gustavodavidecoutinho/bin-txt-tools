# Bin-Txt-Tools 🚀

An open-source, lightweight CLI utility for macOS and Linux to encode and 
decode binary data, text, pixel art matrices, number bases, and bit 
statistics.

## Installation

You can install `bin-txt-tools` globally with a single command:

```bash
pip3 install --user 
git+[https://github.com/gustavodavidecoutinho/bin-txt-tools.git](https://github.com/gustavodavidecoutinho/bin-txt-tools.git)
